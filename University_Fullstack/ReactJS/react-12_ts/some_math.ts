const subtracting  = (a: number, b: number) => {
 let result = a - b;
 console.log(`${a} - ${b} = `+ result);
}

const addition = (a: number, b: number) => {
 let result = a - b;
 console.log(`${a} + ${b} = `+ result);
}


const multiplication = (a: number, b: number) => {
 let result = a * b;
 console.log(`${a} * ${b} = `+ result);
}

const division = (a: number, b: number) => {
 let result = a / b;
 console.log(`${a} / ${b} = `+ result);
}

//
subtracting (2, 1);
addition(9, 5);
multiplication(3, 4);
division(9, 3);
