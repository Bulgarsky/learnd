Init npm (added package json - add script start and etc
npm init -y

install TS
npm i -D typescript

Init TS (added tsconfig - add rules, outDir, include, exclude)
./node_modules/.bin/tsc -init

npm start